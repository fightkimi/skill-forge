# ClipMind Skill 不可修改边界

这份边界来自 ClipMind 当前的真实业务链路、运行契约和 SkillLab 设计，不是通用 Prompt 经验。

## 一句话原则

操盘手可以调“这个 Skill 怎么判断、怎么组织、怎么表达得更好”，不能调“这个 Skill 是谁、何时被调用、吃什么字段、交付什么字段、能调用什么、谁做最终决定”。

## 所有 Skill 共同冻结的内容

| 冻结层 | 不能改什么 | 为什么 |
| --- | --- | --- |
| 身份与路由 | 技术 ID、业务身份、启停状态、分类、模块、SOP 阶段、市场/货架归属 | 工作台靠它找到正确能力，改变后会串流程 |
| 业务作用 | 触发场景、不触发场景、职责范围、上游和下游关系 | 这是产品能力定义，不是文风偏好 |
| IP/global 范围 | 模块的 IP-bound 或 global 属性、租户/IP 权限要求 | 关系到数据隔离与页面上下文 |
| 输入契约 | 必填/选填字段、字段名、类型、占位变量、缺料语义 | 调用方按现有契约传值 |
| 输出契约 | Schema、顶层字段、类型、必需章节、deliverable 名称和状态语义 | 下游页面、服务和测试按它读取 |
| 工具与权限 | 必需工具、条件工具、调用条件、permissions | 改动可能引入真实外部动作或越权 |
| 人工决策边界 | `requires_human_review=true`，策略、计划、内容采用、发布、投放、复盘、方法入库由人工确认 | ClipMind 的 AI 只给草案和建议 |
| 证据与安全 | 不编造、证据缺口、风险、来源追溯、禁止自动发布/评论/投放/写库/改状态 | 防止把假设写成事实或越过业务红线 |
| 来源与基线 | `references/original.md`、source-map、平台定义标记、原始 ID | 用于回收、三方对比和防漂移 |

人工边界不是“只能加强不能削弱”，而是**语义完全冻结**。擅自加强同样会改变业务行为，例如把可安全生成的草案错误阻断。

## 不同来源的具体边界

### 1. Agent 内容 Skill（57 个）

不能改：

- `id/name/status/category/workflow_category/sop_stage/risk_level/common` 等身份和货架信息。
- tools、required_tools、permissions。
- input schema、output schema、必需 deliverable 名称。
- `summary / inputs_used / deliverables / evidence_gaps / risks / next_actions / requires_human_review` 共同契约。
- 角色职责、适用场景、必需输入、护栏和人工确认边界。

可以按反馈最小修改：

- 既有职责内的判断顺序、专业方法、检查步骤。
- 质量标准的清晰度和可执行性。
- 示例、正反例、表达方式、信息组织和自然度。
- 缺料时如何提出更准确的最少补充问题，但不能改变必填字段。

### 2. SOP Prompt（40 个）

其中 31 个是独立业务输出，9 个 `core/*` 是会影响多个宿主任务的共享质量组件。

不能改：

- frontmatter 中的 `skill / prompt_key / module_key / sop_stage / output_schema / output_type / guardrails / eval_checklist / activate_on_seed`。
- `{{variable}}` 输入变量集合。
- JSON 顶层字段、字段类型、必需状态和人工确认字段。
- Prompt 绑定关系、模块链路和线上启用方式。
- `version/change_note` 也不由操盘手修改；开发合入真实源文件时处理版本。

可以按反馈最小修改：

- 原执行正文中的判断方法、注意力顺序、专业问题、同类正反例。
- 在原 Schema 内提高完整性、可读性、IP 贴合度和执行性。

共享 `core/*` 组件改一句可能影响多类内容，必须至少用 3 个不同宿主任务回归，不能只看一条结果就确认。

### 3. 生图 Skill（22 个）

不能改：ID、名称、分类、用途、默认尺寸、支持的尺寸语义、`{subject}` 变量、生成服务边界和“未生成时不得伪装已生成”的规则。

可以改：`原始生图 Prompt` 内的构图、风格、色彩、信息层级、文字安全区、材质、光线和负面约束。必须用同一主题和素材重新看图，而不是只看 Prompt 文本。

### 4. 治理 Skill（27 个）

治理 Skill 是质量镜和评审器，不是直接内容生成 Prompt。当前治理注册定义承载输入输出合同、质量规则、护栏、适用模块、页面展示和方法锚点；现有运行中它也不会直接注入 LLM Prompt。

因此操盘手项目内一律只读：

- 可以拿真实候选内容检验它是否判断准确。
- 可以记录“误判、漏判、解释不清、页面信息不合适”等反馈。
- 不直接修改其 `SKILL.md` 或注册定义。
- 如确需变化，输出工程变更建议单，由产品/开发核对模块链路、契约快照和回归测试。

## IP 上下文的正确理解

- 客户档案、策略、计划、生产和素材库是 IP-bound。
- Agent 工坊本身是 global 能力；当它服务当前 IP 时，必须尊重当前 IP 权限和材料，但不能把这个全局 Skill 改成某个 IP 专属 Skill。
- 发布、监控、复盘、方法库等模块是 global 模块，但具体任务仍可携带当前项目/IP 上下文。
- 所有 Skill 都可用同一个 IP 作为调试样本；这不等于把所有 Skill 的产品归属改成 IP-bound。

## 每轮只改反馈涉及的板块

1. 先保留操盘手明确说好的部分。
2. 把不满意反馈定位到已有板块。
3. 当前轮最多修改直接相关的板块，不做顺手优化。
4. 同一材料复跑，检查问题是否改善、是否伤到已确认能力。
5. 只有跨案例可复用的方法进入 Skill；当前 IP 个案留在档案或本轮输入。

任何需要改变冻结层的反馈都不是“Skill 调优”，而是 ClipMind 产品/工程变更，应另开任务处理。

interface:
  display_name: "ClipMind Skill 调试引导"
  short_description: "按业务依赖逐个调试，并持续显示当前 Skill 与上游产物"
  default_prompt: "使用 $clipmind-skill-tuner 按依赖开始下一个 ClipMind Skill，并先显示当前调试卡。"

policy:
  allow_implicit_invocation: true

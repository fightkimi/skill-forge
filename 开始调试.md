# 开始调试

## 第一次使用

1. 把这个项目导入 Codex。
2. 把一个 IP 的所有原始材料放进 `IP素材库/原始材料/`。文档、表格、图片、音视频、转写文本都可以；不要提前改名或润色。
3. 新建 Codex 对话，发送：

   > 请阅读《开始调试.md》，帮我完成 IP 建档并开始第一个 Skill。

Codex 会先建立材料清单、IP 档案和证据索引。你确认档案后，才会进入 Skill 调试。

## 每个 Skill 单独开一个对话

从第二个 Skill 起，新建 Codex 对话并发送：

> 开始调试下一个 Skill

Codex 会从本地进度自动找到下一项，不需要你记名字或技术 ID。

## 你只需要反馈结果

每轮输出后，Codex 都会给出两个固定选项：

1. `OK，完成这个 Skill`
2. `不OK：这里写你的修改意见`

选择“不OK”时，可以直接说业务感受，例如：

- 开头太慢，前三句没有抓住目标用户。
- 方法是对的，但不像这个 IP 本人会说的话。
- 只讲了结论，没有告诉运营下一步怎么执行。
- 这段太像模板，应该先判断用户所处阶段。
- 保留第二部分，其余重做。

Codex 会先判断反馈能否写进通用 Skill：

- 只属于当前 IP 的事实、产品、案例、口头禅，只用于本轮输出，不写进 Skill。
- 可跨案例复用的方法，才会最小修改当前 Skill。
- 涉及字段、权限、工具、模块归属、人工确认或安全护栏的要求会被拦截，不会硬改。

## 什么时候算完成

只有你明确回复 `OK，完成这个 Skill` 才会收口。Codex 会：

1. 校验不可修改边界。
2. 生成最终 `SKILL.md`、可合入定义、前后差异和验证结果。
3. 标记当前 Skill 完成。
4. 提醒你新开对话调下一个。

治理 Skill 是例外：它们只负责评审，不直接生产内容，也不直接修改。发现问题时，最终交付的是工程变更建议单。

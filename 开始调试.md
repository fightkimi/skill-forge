# 开始调试

## 第一次使用

1. 用 Codex 打开整个项目。
2. 新建一个对话，发送：

   > 请阅读《开始调试.md》，使用小月演练资料开始第一个文案 Skill。

3. Codex 会自动完成：
   - 初始化 32 个纯文案 Skill 的本地进度。
   - 在素材库为空时装载小月 MOCK 演练资料。
   - 读取当前 Skill 对应的固定任务和材料。
   - 模拟工作台执行 Skill，输出首轮完整内容。

不需要上传材料、不需要选择 Skill，也不需要复制技术 ID。

## 小月资料是什么

小月是一套专门用于调优的合成 IP，不对应真实个人。经历、产品、案例、评论和表现数据都明确标注为 MOCK，只能用于本项目演练，不能用于宣传或证明真实业绩。

资料包不是一份简历式薄档案：它包含两轮人物深访、12 组用户访谈、24 条评论/私信、5 个正反案例、完整表达语料、12 份历史内容样本、24 条合成表现数据、六周计划和 36 个候选选题，并通过证据索引标记强弱、冲突与缺口。

如果 `IP素材库/` 已有用户自己的材料，初始化不会覆盖或混入小月资料。

## 每个 Skill 单独开一个对话

从第二个 Skill 起，新建 Codex 对话并发送：

> 开始调试下一个 Skill

Codex 会从本地进度自动找到下一项，并继续使用该 Skill 固定的任务和材料。

## 你只需要反馈文案结果

每轮输出后固定二选一：

1. `OK，完成这个 Skill`
2. `不OK：这里写你的修改意见`

可以直接说业务感受：

- 开头太慢，前三句没有抓住目标用户。
- 判断是对的，但不像小月会说的话。
- 只讲结论，没有给操盘手下一步动作。
- 这段太像模板，应先判断用户处于哪个阶段。
- 保留第二部分，其余重新组织。

Codex 会自动区分：

- 只属于小月的事实、产品、案例和口头禅：只影响本轮结果，不写进通用 Skill。
- 跨 IP 可复用的文案方法：最小修改当前 Skill。
- 涉及身份、结构、字段、Schema、工具、权限、人工边界和安全护栏：直接拦截。
- 涉及视觉、画面、版式、生图、治理、发布或数据：不进入本项目调优。

## 每次修改后会发生什么

1. 先检查当前 Skill 是否在 32 个默认文案队列。
2. 核对原始基线哈希和关键结构。
3. 校验通过后，用同一任务和材料重新生成。
4. 对比已改善、未改善和新问题。
5. 再次让你选择 OK 或不OK。

不会通过更换题目或材料制造“看起来变好了”。

## 什么时候算完成

只有你明确回复 `OK，完成这个 Skill`，Codex 才会：

1. 再次校验不可修改边界。
2. 生成最终 `SKILL.md`、可合入定义、前后差异和验证结果。
3. 标记当前 Skill 完成。
4. 提醒你新建对话调下一个 Skill。

最终回收包不会自动上线 ClipMind。

interface:
  display_name: "视频底层逻辑提炼"
  short_description: "用于视频底层逻辑提炼，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-sop-method-s10-method-underlying-logic，基于当前 IP 案例执行“视频底层逻辑提炼”。"
policy:
  allow_implicit_invocation: false

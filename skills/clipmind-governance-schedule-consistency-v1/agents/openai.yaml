interface:
  display_name: "发布排期一致性"
  short_description: "用于发布排期一致性，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-governance-schedule-consistency-v1，基于当前 IP 案例执行“发布排期一致性”。"
policy:
  allow_implicit_invocation: false

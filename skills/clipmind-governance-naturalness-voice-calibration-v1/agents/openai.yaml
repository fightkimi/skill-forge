interface:
  display_name: "自然度审校与 IP 语气校准"
  short_description: "用于自然度审校与 IP 语气校准，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-governance-naturalness-voice-calibration-v1，基于当前 IP 案例执行“自然度审校与 IP 语气校准”。"
policy:
  allow_implicit_invocation: false

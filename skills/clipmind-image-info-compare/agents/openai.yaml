interface:
  display_name: "对比图"
  short_description: "用于对比图，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-info-compare，基于当前 IP 案例执行“对比图”。"
policy:
  allow_implicit_invocation: false

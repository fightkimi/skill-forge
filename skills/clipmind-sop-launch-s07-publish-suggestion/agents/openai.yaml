interface:
  display_name: "发布建议"
  short_description: "用于发布建议，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-sop-launch-s07-publish-suggestion，基于当前 IP 案例执行“发布建议”。"
policy:
  allow_implicit_invocation: false

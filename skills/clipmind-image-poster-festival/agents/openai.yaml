interface:
  display_name: "节日海报"
  short_description: "用于节日海报，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-poster-festival，基于当前 IP 案例执行“节日海报”。"
policy:
  allow_implicit_invocation: false

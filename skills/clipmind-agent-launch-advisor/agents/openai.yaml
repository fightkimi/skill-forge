interface:
  display_name: "发布排期顾问"
  short_description: "用于发布排期顾问，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-agent-launch-advisor，基于当前 IP 案例执行“发布排期顾问”。"
policy:
  allow_implicit_invocation: false

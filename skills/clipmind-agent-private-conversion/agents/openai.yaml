interface:
  display_name: "私域转化顾问"
  short_description: "用于私域转化顾问，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-agent-private-conversion，基于当前 IP 案例执行“私域转化顾问”。"
policy:
  allow_implicit_invocation: false

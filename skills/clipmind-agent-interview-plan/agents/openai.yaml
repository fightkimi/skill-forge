interface:
  display_name: "采访方案生成器"
  short_description: "用于采访方案生成器，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-agent-interview-plan，基于当前 IP 案例执行“采访方案生成器”。"
policy:
  allow_implicit_invocation: false

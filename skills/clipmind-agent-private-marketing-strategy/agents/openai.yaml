interface:
  display_name: "私域营销策略助手"
  short_description: "用于私域营销策略助手，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-agent-private-marketing-strategy，基于当前 IP 案例执行“私域营销策略助手”。"
policy:
  allow_implicit_invocation: false

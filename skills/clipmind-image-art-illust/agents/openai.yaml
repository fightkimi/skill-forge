interface:
  display_name: "概念插画"
  short_description: "用于概念插画，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-art-illust，基于当前 IP 案例执行“概念插画”。"
policy:
  allow_implicit_invocation: false

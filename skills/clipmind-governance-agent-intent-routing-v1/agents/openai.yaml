interface:
  display_name: "Agent 意图路由"
  short_description: "用于Agent 意图路由，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-governance-agent-intent-routing-v1，基于当前 IP 案例执行“Agent 意图路由”。"
policy:
  allow_implicit_invocation: false

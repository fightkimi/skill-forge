interface:
  display_name: "IP 一致性判断"
  short_description: "用于IP 一致性判断，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-governance-ip-consistency-v1，基于当前 IP 案例执行“IP 一致性判断”。"
policy:
  allow_implicit_invocation: false

interface:
  display_name: "内容改写润色助手"
  short_description: "用于内容改写润色助手，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-agent-content-rewrite-polish，基于当前 IP 案例执行“内容改写润色助手”。"
policy:
  allow_implicit_invocation: false

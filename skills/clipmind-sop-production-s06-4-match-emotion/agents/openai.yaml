interface:
  display_name: "情绪匹配"
  short_description: "用于情绪匹配，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-sop-production-s06-4-match-emotion，基于当前 IP 案例执行“情绪匹配”。"
policy:
  allow_implicit_invocation: false

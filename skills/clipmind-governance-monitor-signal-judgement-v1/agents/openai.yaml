interface:
  display_name: "监控信号判断"
  short_description: "用于监控信号判断，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-governance-monitor-signal-judgement-v1，基于当前 IP 案例执行“监控信号判断”。"
policy:
  allow_implicit_invocation: false

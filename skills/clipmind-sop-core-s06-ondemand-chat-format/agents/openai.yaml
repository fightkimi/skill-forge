interface:
  display_name: "对话格式规范"
  short_description: "用于对话格式规范，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-sop-core-s06-ondemand-chat-format，基于当前 IP 案例执行“对话格式规范”。"
policy:
  allow_implicit_invocation: false

interface:
  display_name: "二次元角色"
  short_description: "用于二次元角色，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-char-anime，基于当前 IP 案例执行“二次元角色”。"
policy:
  allow_implicit_invocation: false

interface:
  display_name: "IP 陪跑简报"
  short_description: "用于IP 陪跑简报，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-sop-companion-briefing，基于当前 IP 案例执行“IP 陪跑简报”。"
policy:
  allow_implicit_invocation: false

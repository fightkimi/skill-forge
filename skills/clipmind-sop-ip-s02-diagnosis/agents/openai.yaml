interface:
  display_name: "业务诊断"
  short_description: "用于业务诊断，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-sop-ip-s02-diagnosis，基于当前 IP 案例执行“业务诊断”。"
policy:
  allow_implicit_invocation: false

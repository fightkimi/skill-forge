interface:
  display_name: "漫画分镜"
  short_description: "用于漫画分镜，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-art-comic，基于当前 IP 案例执行“漫画分镜”。"
policy:
  allow_implicit_invocation: false

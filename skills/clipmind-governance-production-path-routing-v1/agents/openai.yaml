interface:
  display_name: "生产路径判断"
  short_description: "用于生产路径判断，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-governance-production-path-routing-v1，基于当前 IP 案例执行“生产路径判断”。"
policy:
  allow_implicit_invocation: false

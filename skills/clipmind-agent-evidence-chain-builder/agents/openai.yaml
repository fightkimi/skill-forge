interface:
  display_name: "证据链生成助手"
  short_description: "用于证据链生成助手，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-agent-evidence-chain-builder，基于当前 IP 案例执行“证据链生成助手”。"
policy:
  allow_implicit_invocation: false

interface:
  display_name: "IP 形象"
  short_description: "用于IP 形象，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-char-ip，基于当前 IP 案例执行“IP 形象”。"
policy:
  allow_implicit_invocation: false

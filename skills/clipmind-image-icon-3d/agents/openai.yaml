interface:
  display_name: "3D 图标"
  short_description: "用于3D 图标，支持基于当前IP材料独立生成、分析、检查与调优"
  default_prompt: "使用 $clipmind-image-icon-3d，基于当前 IP 案例执行“3D 图标”。"
policy:
  allow_implicit_invocation: false
